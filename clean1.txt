def calculate_sum(numbers):  
    """  
    Calculates the sum of a list of numbers.  
    """  
    if numbers:  
        total = sum(numbers)  
        return total  
    else:  
        return 0